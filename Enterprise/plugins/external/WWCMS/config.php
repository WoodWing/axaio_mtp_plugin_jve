<?php
// URL to the Drupal server including / (e.g. http://localhost/Drupal/)
if( !defined('DRUPAL_URL') ) {
	define( 'DRUPAL_URL', 'http://community.woodwing.net/' );
}

// Drupal username. Needs to have rights to create, update and delete (image) nodes
if( !defined('DRUPAL_USERNAME') ) {
	define( 'DRUPAL_USERNAME', '' );
}

// Drupal password
if( !defined('DRUPAL_PASSWORD') ) {
	define( 'DRUPAL_PASSWORD', '' );
}